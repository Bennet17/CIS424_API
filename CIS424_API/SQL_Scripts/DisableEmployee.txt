USE [capstone_db_01]
GO

SET ANSI_NULLS ON

GO

SET QUOTED_IDENTIFIER ON

GO

CREATE RPOCEDURE [dbo].[sp_DisableEmployee]
    @ID INT
AS
BEGIN
    SET NOCOUNT ON;

    UPDATE Employee
    SET enabled = 0
    WHERE ID = @ID;
END;
GO