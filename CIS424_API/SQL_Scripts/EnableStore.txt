USE [capstone_db_01]
GO

SET ANSI_NULLS ON

GO

SET QUOTED_IDENTIFIER ON

GO

CREATE RPOCEDURE [dbo].[sp_EnableStore]
    @ID INT
AS
BEGIN
    SET NOCOUNT ON;

    UPDATE Store
    SET enabled = 1
    WHERE StoreID = @ID;
END;
GO