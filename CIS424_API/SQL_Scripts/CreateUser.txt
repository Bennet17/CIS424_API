USE [capstone_db_01]
GO

/** Object:  StoredProcedure [dbo].[sp_CreateUser]    Script Date: 1/27/2024 1:08:07 PM **/
SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE PROCEDURE [dbo].[sp_CreateUser]
    @username VARCHAR(255),
    @password VARCHAR(255),
    @role VARCHAR(50),
    @location VARCHAR(100),
    @ResultMessage VARCHAR(255) OUTPUT
AS
BEGIN
    SET NOCOUNT ON;

    -- Check if the username already exists
    IF NOT EXISTS (SELECT 1 FROM users WHERE username = @username)
    BEGIN
        -- Insert the new user into the Users table
        INSERT INTO users (username, password, role, location)
        VALUES (@username, @password, @role, @location);

        SET @ResultMessage = 'User created successfully.';
    END
    ELSE
    BEGIN
        SET @ResultMessage = 'Username already exists. Please choose a different username.';
    END
END;
GO